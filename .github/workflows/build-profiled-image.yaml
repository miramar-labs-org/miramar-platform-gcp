name: Build Profiled PyTorch Image

on:
  workflow_dispatch:
    inputs:
      runner:
        description: "Target machine (dgx = DGX Spark, agx = AGX Orin)"
        type: choice
        required: true
        default: dgx
        options:
          - dgx
          - agx

permissions:
  contents: read
  packages: write

jobs:
  build:
    runs-on: [self-hosted, "${{ inputs.runner }}"]
    steps:
      - uses: actions/checkout@v4

      - name: Log in to GHCR
        run: |
          echo "${{ secrets.GITHUB_TOKEN }}" \
            | docker login ghcr.io -u "${{ github.actor }}" --password-stdin

      - name: Build and push profiled PyTorch image
        run: |
          IMAGE="ghcr.io/miramar-labs-org/pytorch-profiled:25.03"
          docker build -f docker/Dockerfile.profiled -t "$IMAGE" docker/
          docker push "$IMAGE"
          echo "Pushed $IMAGE"
