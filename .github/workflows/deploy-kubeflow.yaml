name: Kubeflow Deploy

# Deploys Kubeflow Pipelines standalone into the kubeflow namespace on the target
# k3s cluster. The env/dev kustomize overlay creates the kubeflow namespace
# automatically and includes built-in SeaweedFS and MySQL — no external dependencies.
# arm64 image overrides applied post-apply:
#   - controller-manager removed (Application CRD, display-only, amd64-only binary)
#   - All upstream amd64-only images replaced with ghcr.io/miramar-labs-org arm64 builds
#     when use_native_kfp_images=true (the default)
#
# Prerequisites (run in order before first deploy or after version bump):
#   1. Build KFP arm64 Images (covers all 13 components including MLMD)
# See dgx/k3s/kubeflow/arm64/README.md for details.
#
# UI is served by svc/ml-pipeline-ui:80, forwarded to host port 8080 by
# kubeflow-portfwd.service. The REST API is served by svc/ml-pipeline:8888,
# forwarded to host port 8890 by kfp-api-portfwd.service. Restart both after
# deploying:
#   systemctl --user restart kubeflow-portfwd kfp-api-portfwd
#
# Prerequisites:
#   - k3s running on the target machine (K3s Install workflow)
#   - kubeflow-portfwd.service and kfp-api-portfwd.service installed

on:
  workflow_dispatch:
    inputs:
      runner:
        description: "Target machine (dgx = DGX Spark, agx = AGX Orin)"
        type: choice
        required: true
        default: dgx
        options:
          - dgx
          - agx
      pipeline_version:
        description: Kubeflow Pipelines version tag (e.g. 2.16.1)
        default: "2.16.1"
        required: false
      use_native_kfp_images:
        description: Patch all KFP deployments with native arm64 images (requires Build KFP arm64 Images to have run)
        type: boolean
        default: true
        required: false

permissions:
  contents: read
  packages: write

jobs:
  deploy:
    runs-on: [self-hosted, "${{ inputs.runner }}"]
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

      - name: Resolve host
        run: |
          if [[ "${{ inputs.runner }}" == "agx" ]]; then
            echo "HOST_IP=${{ vars.AGX_HOST_IP }}"   >> "$GITHUB_ENV"
            echo "HOST_USER=${{ vars.AGX_HOST_USER }}" >> "$GITHUB_ENV"
          else
            echo "HOST_IP=${{ vars.DGX_HOST_IP }}"   >> "$GITHUB_ENV"
            echo "HOST_USER=${{ vars.DGX_HOST_USER }}" >> "$GITHUB_ENV"
          fi

      - name: Set up SSH key
        run: |
          echo "${{ secrets.HOST_SSH_KEY }}" > /tmp/k_key
          chmod 600 /tmp/k_key
          ssh-keyscan -H "${HOST_IP}" >> ~/.ssh/known_hosts 2>/dev/null || true

      - name: Copy kubeconfig from host
        run: |
          mkdir -p ~/.kube
          scp -i /tmp/k_key -o StrictHostKeyChecking=no \
            "${HOST_USER}@${HOST_IP}:/home/${HOST_USER}/.kube/config" ~/.kube/config
          echo "KUBECONFIG=${HOME}/.kube/config" >> "$GITHUB_ENV"

      - name: Verify cluster access
        run: kubectl get nodes

      - name: Prepare KFP data directories on host
        run: |
          ssh -i /tmp/k_key -o StrictHostKeyChecking=no "${HOST_USER}@${HOST_IP}" "
            mkdir -p /home/${HOST_USER}/shared/kfp/mysql /home/${HOST_USER}/shared/kfp/seaweedfs
            chmod 777 /home/${HOST_USER}/shared/kfp/mysql /home/${HOST_USER}/shared/kfp/seaweedfs || true
          "

      - name: Log in to GHCR
        run: |
          echo "${{ secrets.GITHUB_TOKEN }}" \
            | docker login ghcr.io -u "${{ github.actor }}" --password-stdin

      - name: Deploy Kubeflow Pipelines
        env:
          PIPELINE_VERSION: ${{ inputs.pipeline_version || '2.16.1' }}
          MLMD_SERVER_VERSION: "1.14.0"
          USE_NATIVE_KFP_IMAGES: ${{ inputs.use_native_kfp_images == false && 'false' || 'true' }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_ACTOR: ${{ github.actor }}
          MIRAMAR_ORG_GHCR_PAT: ${{ secrets.MIRAMAR_ORG_GHCR_PAT }}
        run: |
          KFP_DATA_DIR="/home/${HOST_USER}/shared/kfp" \
          bash dgx/k3s/kubeflow/deploy-kubeflow.sh

      - name: Wait for pods
        run: |
          echo "Waiting up to 20m for all pods to reach Running/Completed..."
          timeout 1200 bash -c '
            pull_fail_iters=0
            while true; do
              statuses="$(kubectl get pods -n kubeflow --no-headers 2>/dev/null)"
              if echo "$statuses" | grep -qE "ImagePullBackOff|ErrImagePull"; then
                pull_fail_iters=$((pull_fail_iters + 1))
                # Tolerate transient pull failures for up to 5 minutes (10 x 30s)
                # before declaring fatal — slow GHCR responses are common on first pull.
                if [[ $pull_fail_iters -ge 10 ]]; then
                  echo "ERROR: image pull failure persisted for 5m — giving up" >&2
                  echo "$statuses" >&2
                  exit 1
                fi
                echo "$(date -u +%H:%M:%S) — image pull backoff on $(echo "$statuses" | grep -cE "ImagePullBackOff|ErrImagePull") pod(s) [attempt ${pull_fail_iters}/10, waiting...]"
              else
                pull_fail_iters=0
              fi
              not_ready="$(echo "$statuses" | grep -vE "Running|Completed|Succeeded" | grep -v "^$")"
              if [[ -z "$not_ready" ]]; then
                echo "All pods ready."
                break
              fi
              echo "$(date -u +%H:%M:%S) — waiting on $(echo "$not_ready" | wc -l) pod(s):"
              echo "$not_ready" | awk "{print \"  \", \$1, \$3}" | head -5
              sleep 30
            done
          '
          kubectl get pods -n kubeflow

      - name: Create KFP shared storage
        run: |
          # --- Host directories: create via SSH (not mounted in runner container) ---
          # ~/shared/nsight is deliberately NOT created here: it is the profiling
          # archive root, owned by scripts/nsight/export-report.sh (which mkdir -p's
          # the full destination), not by KFP storage provisioning.
          ssh -i /tmp/k_key -o StrictHostKeyChecking=no "${HOST_USER}@${HOST_IP}" "
            mkdir -p /home/${HOST_USER}/shared/huggingface-kfp/hub \
                     /home/${HOST_USER}/shared/huggingface-kfp/adapters
          "

          # --- PVs: skip if already exist (spec.persistentvolumesource is immutable after creation)
          kubectl get pv hf-model-cache &>/dev/null || kubectl apply -f - <<EOF
          apiVersion: v1
          kind: PersistentVolume
          metadata:
            name: hf-model-cache
          spec:
            capacity:
              storage: 200Gi
            accessModes: [ReadWriteMany]
            persistentVolumeReclaimPolicy: Retain
            storageClassName: ""
            hostPath:
              path: /home/${HOST_USER}/shared/huggingface-kfp
          EOF
          # --- Retire the legacy nsight-reports PV/PVC ---
          # It dates from a removed profiling mechanism (an nsys-wrapper entrypoint
          # image that cp'd reports into the hostPath). The Nsight Operator replaced
          # that path entirely: it writes to its own MinIO, and
          # scripts/nsight/export-report.sh writes the host archive directly.
          #
          # Nothing has mounted it since. Verified on the DGX 2026-09-07: no pod in any
          # namespace referenced the claim, and there are zero references in templates/
          # or any project repo — only the workflows that created it.
          #
          # Its hostPath was /home/$HOST_USER/shared/nsight — the profiling archive ROOT,
          # RWX. Any pod that mounted it would have had read-write access to every
          # captured report in the archive. Inert only because nothing mounted it.
          #
          # This cleanup is idempotent and can be dropped once dgx/agx have each
          # redeployed past this commit.
          kubectl delete pvc nsight-reports -n kubeflow --ignore-not-found --wait --timeout=120s
          kubectl delete pv nsight-reports --ignore-not-found --wait --timeout=120s

          # --- PVCs: always apply (no immutable fields; idempotent)
          kubectl apply -f - <<'EOF'
          apiVersion: v1
          kind: PersistentVolumeClaim
          metadata:
            name: hf-model-cache
            namespace: kubeflow
          spec:
            accessModes: [ReadWriteMany]
            resources:
              requests:
                storage: 200Gi
            storageClassName: ""
            volumeName: hf-model-cache
          EOF

          kubectl get pv hf-model-cache 2>/dev/null || true
          kubectl get pvc hf-model-cache -n kubeflow 2>/dev/null || true

      - name: Smoke-test endpoints
        continue-on-error: true
        run: bash dgx/k3s/kubeflow/verify-kfp-endpoints.sh

      - name: Sync API keys to cluster
        env:
          OPENAI_API_KEY:    ${{ secrets.OPENAI_API_KEY }}
          HF_TOKEN:          ${{ secrets.HF_TOKEN }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          WANDB_API_KEY:     ${{ secrets.WANDB_API_KEY }}
          LANGCHAIN_API_KEY: ${{ secrets.LANGCHAIN_API_KEY }}
          NGC_API_KEY:       ${{ secrets.NGC_API_KEY }}
          NVIDIA_API_KEY:    ${{ secrets.NVIDIA_API_KEY }}
        run: |
          kubectl create secret generic mlabs-api-keys \
            --from-literal=OPENAI_API_KEY="$OPENAI_API_KEY" \
            --from-literal=HF_TOKEN="$HF_TOKEN" \
            --from-literal=ANTHROPIC_API_KEY="$ANTHROPIC_API_KEY" \
            --from-literal=WANDB_API_KEY="$WANDB_API_KEY" \
            --from-literal=LANGCHAIN_API_KEY="$LANGCHAIN_API_KEY" \
            --from-literal=NGC_API_KEY="$NGC_API_KEY" \
            --from-literal=NVIDIA_API_KEY="$NVIDIA_API_KEY" \
            -n kubeflow --dry-run=client -o yaml | kubectl apply -f -

      - name: Set KFP active state
        env:
          GH_TOKEN: ${{ secrets.MIRAMAR_ORG_ADMIN_PAT }}
        run: |
          machine=$(echo "${{ inputs.runner }}" | tr '[:lower:]' '[:upper:]')
          var="${machine}_KFP_ACTIVE"
          gh api --method PATCH "/orgs/miramar-labs-org/actions/variables/${var}" \
            -f name="${var}" -f value="true" -f visibility="all" \
          || gh api --method POST "/orgs/miramar-labs-org/actions/variables" \
            -f name="${var}" -f value="true" -f visibility="all"

      - name: Cleanup SSH key
        if: always()
        run: rm -f /tmp/k_key

      - name: Summary
        if: always()
        run: |
          POD_COUNT=$(kubectl get pods -n kubeflow --no-headers 2>/dev/null | grep -c . || echo 0)
          {
            echo "## Kubeflow Pipelines Deployed"
            echo "| | |"
            echo "|---|---|"
            echo "| Runner | \`${{ inputs.runner }}\` |"
            echo "| Namespace | \`kubeflow\` |"
            echo "| Version | \`${{ inputs.pipeline_version || '2.16.1' }}\` |"
            echo "| Pods | \`${POD_COUNT}\` |"
            echo ""
            echo "Restart port-forward services on the host, then tunnel to access:"
            echo "\`\`\`"
            echo "systemctl --user restart kubeflow-portfwd kfp-api-portfwd"
            echo "ssh -L 8080:localhost:8080 -L 8890:localhost:8890 <user>@<host>"
            echo "# UI:  http://localhost:8080"
            echo "# API: http://localhost:8890/apis/v2beta1/healthz"
            echo "\`\`\`"
          } >> "$GITHUB_STEP_SUMMARY"
