name: WSL2 Post-Provision

# Per-distro SSH mesh setup. Run after WSL2 Provision.
#
# Windows side (post-provision.ps1):
#   1. .wslconfig (mirrored networking) + wsl --shutdown if changed
#   2. Firewall rule for sshd port
#   3. sshd port config inside distro
#   4. Write .smbcredentials + /etc/wsl2-distro-name inside distro
#   5. Install setup-shared-ssh.sh + enable wsl2-ssh-setup.service
#   6. Run setup-shared-ssh.sh (initial sync via smbclient, no CIFS mount)
#
# setup-shared-ssh.sh (runs inside the distro):
#   - Downloads config / known_hosts / authorized_keys from DGX shared store
#   - Adds own pubkey → uploads authorized_keys back
#   - Adds wsl2-<name> host block → uploads config back
#   - On every subsequent cold start: wsl2-ssh-setup.service re-runs it
#
# Prerequisites:
#   - WSL2 distro provisioned (run WSL2 Provision first)
#   - Setup Shared SSH Store run at least once (initialises DGX ~/shared/ssh/)
#   - Secrets: WSL2_HOST, WSL2_HOST_USER, WSL2_HOST_SSH_KEY, DGX_SMB_PASSWORD
#   - Vars:    DGX_HOST

on:
  workflow_dispatch:
    inputs:
      distro_name:
        description: "WSL2 distro name to configure"
        required: true
        default: dev
      ssh_port:
        description: "sshd port (2222 for first distro, increment for each additional)"
        required: false
        default: "2222"
      runner:
        description: "Runner to use for the Windows SSH steps"
        required: false
        default: dgx
        type: choice
        options:
          - dgx
          - agx

jobs:

  windows-setup:
    runs-on: ${{ inputs.runner }}
    outputs:
      wsl2_pubkey: ${{ steps.run-script.outputs.wsl2_pubkey }}

    steps:
      - uses: actions/checkout@v4

      - name: Set up SSH key for Windows host
        run: |
          install -m 700 -d ~/.ssh
          printf '%s\n' "${{ secrets.WSL2_HOST_SSH_KEY }}" > ~/.ssh/wsl2_host
          chmod 600 ~/.ssh/wsl2_host
          ssh-keyscan -H "${{ secrets.WSL2_HOST }}" >> ~/.ssh/known_hosts 2>/dev/null || true

      - name: Copy scripts to Windows host
        run: |
          scp -i ~/.ssh/wsl2_host \
            -o BatchMode=yes \
            -o IdentitiesOnly=yes \
            -o PreferredAuthentications=publickey \
            -o PasswordAuthentication=no \
            wsl2/post-provision.ps1 \
            wsl2/setup-shared-ssh.sh \
            "${{ secrets.WSL2_HOST_USER }}@${{ secrets.WSL2_HOST }}:"

      - name: Run post-provision.ps1 on Windows host
        id: run-script
        run: |
          set -euo pipefail
          # tee streams output in real-time; || true ensures we always check for WSL2_PUBKEY.
          PS_OUT=$(mktemp)
          ssh -i ~/.ssh/wsl2_host \
            -o BatchMode=yes \
            -o IdentitiesOnly=yes \
            -o PreferredAuthentications=publickey \
            -o PasswordAuthentication=no \
            -o ServerAliveInterval=30 \
            -o ServerAliveCountMax=10 \
            "${{ secrets.WSL2_HOST_USER }}@${{ secrets.WSL2_HOST }}" \
            "powershell -ExecutionPolicy Bypass -File \"%USERPROFILE%\\post-provision.ps1\" \
              -Name \"${{ inputs.distro_name }}\" \
              -User \"${{ secrets.WSL2_HOST_USER }}\" \
              -Port \"${{ inputs.ssh_port }}\" \
              -DgxHost \"${{ vars.DGX_HOST }}\" \
              -SmbPassword \"${{ secrets.DGX_SMB_PASSWORD }}\"" \
            | tee "$PS_OUT" || true
          WSL2_PUBKEY=$(grep '^WSL2_PUBKEY=' "$PS_OUT" | cut -d= -f2- || true)
          if [[ -z "$WSL2_PUBKEY" ]]; then
            echo "ERROR: post-provision.ps1 did not output WSL2_PUBKEY" >&2
            exit 1
          fi
          echo "wsl2_pubkey=${WSL2_PUBKEY}" >> "$GITHUB_OUTPUT"

  summary:
    needs: windows-setup
    if: always()
    runs-on: ${{ inputs.runner }}
    steps:
      - name: Summary
        run: |
          {
            echo "## WSL2 Post-Provision"
            echo "| | |"
            echo "|---|---|"
            echo "| Distro | \`${{ inputs.distro_name }}\` |"
            echo "| SSH alias | \`wsl2-${{ inputs.distro_name }}\` |"
            echo "| Port | \`${{ inputs.ssh_port }}\` |"
            echo "| Host | \`${{ secrets.WSL2_HOST }}\` |"
            echo ""
            echo "### Result"
            echo "| Step | Status |"
            echo "|---|---|"
            echo "| .wslconfig + firewall + sshd + smbclient SSH mesh sync | ${{ needs.windows-setup.result }} |"
            echo ""
            echo "### Next step"
            echo "Run **WSL2 Verify SSH Topology** to confirm all SSH paths work end-to-end."
          } >> "$GITHUB_STEP_SUMMARY"
